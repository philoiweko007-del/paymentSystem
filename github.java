public class github {
    public static void main(String[] args) {
        double amount = 500;

        double total = amount + 800;
        System.out.println("original amount" + amount);

        if (amount > 600) ;
        System.out.println("Total payment" + amount);
    }
}
